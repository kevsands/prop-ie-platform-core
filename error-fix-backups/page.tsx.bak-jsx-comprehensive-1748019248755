'use client';

import MainNavigation from '@/components/navigation/MainNavigation';

export default function HomeNavTest() {
  return (
    <div>
      <MainNavigation theme="light" isTransparent={false} isHomePageHeader={false} />
      <div style={{ paddingTop: '80px', padding: '20px' }}>
        <h1>Navigation Test Page</h1>
        <p>This page explicitly includes the MainNavigation component.</p>
        <p>If you see the navigation bar above, the component works correctly.</p>
      </div>
    </div>
  );
}